<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmharicCharades">
        <activity
            android:name=".CategoryActivity"
            android:exported="false" />
        <!-- SCORE: Portrait (Easier to read lists) -->
        <activity
            android:name=".ScoreActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- GAME: LOCKED LANDSCAPE (Critical for gyroscope!) -->
        <activity
            android:name=".GameActivity"
            android:exported="false"
            android:screenOrientation="landscape" /> <!-- INSTRUCTIONS: Landscape (Matches the tutorial images) -->
        <activity
            android:name=".InstructionActivity"
            android:exported="false"
            android:screenOrientation="landscape" /> <!-- SETTINGS: Portrait -->
        <activity
            android:name=".GameSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- MAIN MENU: Portrait -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>